import 'package:flutter/material.dart';

class FriendActivity extends StatelessWidget {
  const FriendActivity({
    super.key,
  });

  @override
  Widget build(BuildContext context) {
    return const Text("Row 1 Col 3 Actividad de Amigos");
  }
}